# ui

- The landing page (especially the hero section) must deliver a premium, visually stunning first impression — gradient blurs, animated particles, staggered word/split-text animations, magnetic button effects, and layered depth. The user will explicitly reject landing pages that feel underwhelming or insufficiently polished, calling it out as a separate concern from animation quality. Confidence: 0.90
- Admin-only fields (status flags like "Active", ordering numbers like "Order", file management status like "File") must be strictly segregated from public-facing views. The public user should never see raw backend administration controls or database fields. Any certificate/document/public viewer page should display only presentation-relevant metadata (title, issuer, image, dates, credential link, description) in a polished, professional layout — never exposing admin CRUD fields to site visitors. Confidence: 0.85
- Use a dark "Neural Core" robotics theme with deep black background (#080A10), robot-eye cyan accent (#00F0FF), neural violet (#7C3AED), energy coral (#FF3B6F), and system green (#00E676) for status indicators. Confidence: 0.85

- For profile/hero images, switch shape responsively: circle (`rounded-full`) on mobile viewports and rounded rectangle (`lg:rounded-3xl`/`lg:rounded-2xl`) on desktop (≥1024px). All decorative rings, glow layers, and container borders should follow the same responsive radius pattern so the entire composition stays visually consistent at every breakpoint. Confidence: 0.80


- Use fluid typography with `clamp()` for hero headings (e.g., `text-[clamp(2rem,8vw,3rem)]`) paired with responsive breakpoint classes (`sm:`, `lg:`, `xl:`) to prevent text overflow on small screens while scaling up on large screens. Also use `clamp()` for section padding (e.g., `clamp(16px, 5vw, 24px)` on desktop, `clamp(12px, 4vw, 16px)` on mobile) so horizontal padding scales fluidly with viewport width instead of using fixed pixel breakpoints. On mobile, reduce section vertical padding (e.g., `py-12 sm:py-20` instead of `py-20`) and tighten gaps between elements (`gap-6 sm:gap-10`, `mb-3 sm:mb-6`) for a more compact, polished viewport fit. Confidence: 0.80
- All interactive elements (buttons, icons, toggles, links) must have a minimum touch target of 44x44px (prefer 48px min-height where practical) with comfortable padding — especially on mobile — for thumb-accessible interaction. Add smooth active/scale-down feedback on touch (`active:scale-[0.97]` or `active:scale-[0.98]`) with fast transitions (~150ms) to provide tactile press feedback without feeling sluggish. Confidence: 0.85

- Cards and interactive panels should have a hover lift animation (`y: -4`, `scale: 1.02`) with smooth transitions (0.3s–0.5s) to indicate interactivity and add polish — apply this consistently across all card components (skill cards, project cards, testimonial cards, timeline cards, etc.) for a cohesive premium feel. Confidence: 0.75

- Use animated counters that count up from 0 to their final value when scrolled into view (using `useInView` with `once: true`). The counter animation should use `setInterval` with ~16ms steps for smooth 60fps updates, and the number should spring-scale into view on mount. This pattern applies to any stat/metric display across the site. Confidence: 0.75

- Timeline/experience sections should use a professional vertical timeline layout: a continuous gradient vertical line (left-aligned, ~1px wide, from accent color fading to transparent) with animated pulsing dots at each timeline node (~16-20px diameter, border-2 with accent, filled inner circle), and glass cards positioned to the right of the line. Each card shows duration, role title, company/org, description, and technology badges. Cards should have hover lift animation. This mimics premium portfolios from Apple/Linear/Vercel/Microsoft. Confidence: 0.70

- Tab navigation between content sections (Experience, Skills, Education, Certifications, etc.) should use pill-shaped buttons with icons (`FiBriefcase`, `FiCode`, `FiBookOpen`, `FiAward`) and a gradient active background (`bg-accent text-background` with glow shadow) — not plain text pills or underlined tabs. The active tab should have a distinct visual state with contrasted text color, and inactive tabs should have a subtle border (`border border-white/[0.08]`) and muted text. Use `AnimatePresence` with `mode="wait"` for smooth cross-fade transitions between tab content. Confidence: 0.70

- Separate page sections visually with subtle background gradient transitions (e.g., `bg-gradient-to-b from-transparent via-accent/[0.015] to-transparent`) to create depth and indicate section boundaries — rather than relying solely on vertical padding or hard borders. Confidence: 0.70

- Use a consistent spacing scale system across the entire site: section padding follows `py-12 sm:py-20`, card padding follows `p-4 sm:p-6` or `p-5 sm:p-6`/`p-5 sm:p-8`, and grid gaps follow `gap-3 sm:gap-4` or `gap-4 sm:gap-6`. Maintain a predictable set of spacing values (12, 16, 20, 24, 32, 40, 48, 64 in Tailwind units) rather than ad-hoc spacing per section. Confidence: 0.70
- Sections and layouts should feel **compact but premium** — reduce unnecessary vertical space, avoid oversized cards/inputs/buttons, and prefer tight padding (e.g., 18-24px on cards, 50px input heights, ~175px textarea heights) with consistent 18-22px spacing between form rows. The overall aesthetic should be dense but breathable, not stretched or oversized. Use `min-height` instead of hardcoded `height` wherever possible so containers resize naturally. Confidence: 0.85
- Filter/search bars and their container backgrounds should be compact and tightly packed — use minimal vertical padding (e.g., `p-3 sm:p-4` instead of `p-4 sm:p-6`), small gaps between elements (e.g., `gap-1.5` between filter pills, `gap-3 sm:gap-4` overall), shorter input fields (e.g., `min-h-[40px]` instead of `min-h-[48px]`, `py-2` instead of `py-3`), and smaller pill-type filter buttons (e.g., `px-3.5 sm:px-4 min-h-[34px]` instead of `px-4 sm:px-5 min-h-[40px]`) to avoid a bulky, over-padded appearance. The bar should feel sleek and premium without excess empty vertical space. Confidence: 0.70
- Navigation bars should be slender and compact — not oversized or bulky. Use restrained heights (e.g., `h-14 md:h-16`), small-to-medium logo sizing (e.g., `h-7 md:h-9`, `text-lg md:text-xl`), tight padding on nav links (e.g., `px-3 py-1.5`), compact CTA buttons (e.g., `px-4 py-1.5`), and small hamburger touch targets (e.g., `w-10 h-10`). A too-large navbar feels unprofessional and dominates the viewport unnecessarily. Confidence: 0.85
- In mobile navigation sidebars, prioritize high-value actionable CTAs (e.g., "Download Resume" / "Download CV") over social media follow sections. The limited sidebar real estate should be used for user actions that drive conversion — social media links are better placed in the main site footer or hero section where they don't compete for space with primary CTAs. Remove social follow content from the sidebar entirely rather than conditionally hiding/showing it. The user explicitly expects the Download CV button in both the About page and the mobile sidebar, with the FOLLOW section removed. Confidence: 0.85
- Prevent horizontal overflow on the root layout with `overflow-x-hidden` on the outermost container, and ensure no component forces unwanted horizontal scrollbars on mobile viewports. Confidence: 0.80
- Use responsive aspect ratios for media containers — e.g., `aspect-[16/9] sm:aspect-[21/9]` so hero/banner images don't appear squished on narrow mobile viewports. For portrait/hero images that pair with text in a split layout, constrain the image container width on mobile with `max-w-xs sm:max-w-sm mx-auto` so it doesn't span the full viewport width and looks balanced when stacked below text. Confidence: 0.75
- In portfolio hero/about sections, follow a specific vertical content hierarchy: (1) name/heading first, (2) descriptive text plus a prominent call-to-action button (e.g., Download CV) in the middle, (3) profile photograph centered at the bottom. On mobile, this should stack into a centered single-column layout where textual content (name, description, CTA) always appears above the profile image — the message/CTA takes visual precedence over the photo in the reading order. On desktop, text and image can sit side-by-side. Confidence: 0.80

- Use responsive breakpoint patterns consistently: `flex-col md:flex-row` for stack-to-side layouts, `grid-cols-1 sm:grid-cols-2 md:grid-cols-4` for responsive grids. For hero/section layouts that stack vertically on mobile, center-align text on mobile (`text-center lg:text-left`) so the layout looks balanced when the image sits below the text. Confidence: 0.75 For footers with fewer columns, switch to 2 columns at `sm:` breakpoint instead of waiting for `md:` (e.g., `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`). Hide non-essential labels/descriptions behind `hidden sm:inline` on mobile to keep compact toolbars, and shrink icon/button sizes proportionally on small screens (e.g., `p-1.5 sm:p-2`, `size={14} sm:size={16}`). Confidence: 0.80
- Never use fixed pixel widths (`width: 500px`, `min-w-[600px]`) on containers, cards, or grid items. Always use flexible Tailwind utilities (`w-full`, `max-w-full`, `flex-1`) so layouts scale fluidly on mobile viewports — clamped by parent `max-w-*` boundaries rather than hard-coded widths. For decorative/blobs/orbs that use absolute positioning with large `w-[400px]`/`w-[500px]` dimensions, add `max-w-[100vw]` or `max-w-[200vw]` so they don't push past the viewport edge on mobile. Confidence: 0.80
- When a chatbot, sidebar, modal, or overlay opens, lock body scroll by adding both `overflow: hidden` and `position: fixed` to `<body>`, and save/restore the scroll position (`window.scrollY`) so the page doesn't snap to the top when unlocking. This prevents scroll chaining on touch/mobile devices. Trap focus inside the component, auto-focus the primary input, and allow Escape key to close. Confidence: 0.85

- When the mobile sidebar/overlay menu is open, the floating action buttons (scroll-to-top, chatbot, WhatsApp, etc.) must be hidden entirely — they sit at `z-[999]` above the sidebar (`z-50`) and will always overlap sidebar content (especially social/follow sections at the bottom). Hide them by detecting the sidebar-open class (e.g., polling `document.body.classList.contains('no-scroll')` every ~200ms) and applying `display: none` to the mobile floating column. Do not attempt to re-z-index the sidebar or move the buttons — hiding them is the cleanest solution. Confidence: 0.85
- When the chatbot is open, ALL floating action buttons (scroll-to-top, chatbot toggle, WhatsApp) must be hidden — not just the chatbot toggle. The user expects a clean overlay experience where opening the chatbot panel hides every floating element to prevent visual clutter. The conditional should apply consistently to both mobile and desktop variants of each button in the floating stack. Confidence: 0.90

- Establish and maintain a clear z-index hierarchy across the entire app so no layer unintentionally covers another. The reference hierarchy (from lowest to highest) should be: decorative noise/scene layers (~20), navbar (~30), mobile nav backdrop (~40), mobile nav panel (~50), admin sidebar (~60), scroll-to-top button (~70), chatbot backdrop (~80), chatbot panel (~90), loading screen (~100), floating action buttons/chatbot toggle (~999), custom cursor and toasts (~9999). Every fixed/absolute layer must slot into this system — never use arbitrary high values like 9998 for decorative elements. Confidence: 0.85

- Consolidate all floating action buttons (chatbot toggle, WhatsApp, scroll-to-top, etc.) into a **single** FloatingButtons component rather than scattering them across separate components with independently-positioned buttons. When the chatbot toggle lives in its own component, integrate it into the shared floating column via a hidden trigger element (`id="chatbot-toggle"`) that the floating component clicks programmatically. This ensures the stack is always consistently ordered, spaced, and positioned with one cohesive z-index and one set of responsive breakpoints. Confidence: 0.85

- Floating button stacks (vertical column on right for both desktop and mobile) should maintain consistent vertical spacing between buttons with equal gaps (e.g., `gap-3`), uniform button sizing (e.g., `w-12 h-12`), and a fixed, predictable order from top to bottom. The layout should be the same across viewports — a fixed vertical column on the right side — not a horizontal bottom bar on mobile. The preferred order (top to bottom) is: **Scroll-to-top arrow** (conditional/animated, appears after scrolling past 400px) → **Chatbot robot icon** (always visible unless sidebar is open) → **WhatsApp green icon** (always visible, bottom-most position). Use circular icon-only buttons (48×48px, `rounded-full`) with distinguishable solid backgrounds per type — scroll-to-top gets a dark bordered circle (`border border-white/[0.08] bg-[#111827]`), chatbot gets the accent/cyan background with glow shadow (`bg-accent text-background shadow-[0_0_20px_rgba(0,240,255,0.3)]`), and WhatsApp gets its brand green background with green glow shadow (`bg-[#25D366] text-white shadow-[0_0_20px_rgba(37,211,102,0.25)]`). Use `sm:hidden` to toggle between desktop and mobile variants. Position with `bottom: max(24px, calc(24px + env(safe-area-inset-bottom, 0px)))` and `right: max(16px, calc(16px + env(safe-area-inset-right, 0px)))` for safe-area-aware spacing. This makes each button visually distinct at a glance and keeps the spatial memory consistent across viewports. Confidence: 0.85
- Use glow box-shadows, animated gradient text, animated particles, circuit lines, orbit rings, scanlines, and glass-morphism cards with backdrop blur for a futuristic feel. For a more premium look, prefer gradient backgrounds on cards (`bg-gradient-to-br from-bg-card/80 to-bg-card/30`) instead of solid `bg-bg-card` — this adds subtle visual depth and feels more modern. Confidence: 0.75

- Use a pure-CSS gradient border technique on cards and CTAs via a `::before` pseudo-element with `mask-composite: exclude`: set `before:absolute before:inset-0 before:rounded-2xl before:p-[1px] before:bg-gradient-to-br before:from-white/[0.08] before:to-white/[0.02] before:[-webkit-mask:linear-gradient(#fff_0_0)_content-box,linear-gradient(#fff_0_0)] before:[-webkit-mask-composite:xor] before:[mask-composite:exclude] before:pointer-events-none`. This creates a subtle gradient edge that feels more premium than a flat border without adding extra HTML markup. For accent-colored gradient borders (e.g., on CTA buttons), use a brighter gradient like `before:from-accent/30 before:to-accent-neural/20`. Pair this with `bg-[#111827] shadow-card` as the card background. Confidence: 0.80

- For icon containers on cards (contact info, stat cards, feature highlights), use a gradient background (`bg-gradient-to-br from-accent/15 to-accent-neural/10`) instead of a flat opacity tint (`bg-accent/10`), and pair it with an enhanced glow shadow on hover (`group-hover:shadow-[0_0_24px_rgba(0,240,255,0.2)]` upgrading from `shadow-[0_0_16px_rgba(0,240,255,0.12)]`). This adds visual depth to what would otherwise be flat icon circles/squares. Confidence: 0.80

- For CTA/highlight buttons (e.g., WhatsApp connect bar), use a hover gradient wash overlay: an absolutely positioned inner element (`absolute inset-0 bg-gradient-to-r from-accent/5 to-accent-neural/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300`) that fades in across the entire button surface on hover, instead of just changing the background color. This creates a polished, animated feel on interactive elements. Confidence: 0.80
- For download/resume buttons, use the site's own theme gradient (e.g., `bg-gradient-to-r from-accent to-accent-neural`) with matching glow shadow (`shadow-accent/25`) — NOT a standalone orange color. The button must visually coordinate with all other accent-colored CTAs on the site. Use `rounded-full` pill shape, a minimalist SVG download icon (downward arrow over a flat bar) placed **on the left** of the text, dark `text-background` text, and balanced internal padding (`px-8 min-h-[54px]`) for comfort. Hover effects should be subtle — `hover:brightness-110`, `hover:scale-[1.02]` with stronger matching shadow. Active press feedback with `active:scale-[0.97]`. Avoid shimmer sweep animations, frosted glass icon containers, and complex hover animations — the aesthetic should be clean and minimalist while matching the site's theme. Confidence: 0.90
- The user notices and explicitly rejects UI elements that don't match the site's theme/color scheme — all buttons, CTAs, and interactive elements must use the site's own accent gradient and colors, not standalone/off-theme colors (e.g., orange #FF8C42 that doesn't belong to the dark Neural Core theme). When fixing such mismatches, proactively find and fix the same issue across all instances (e.g., both the About page and the sidebar download button). Confidence: 0.90

- **CRITICAL: Never apply `backdrop-filter`, `backdrop-blur`, or glass-morphism effects to any container that holds readable text** — these compositing/GPU-layer effects destroy subpixel antialiasing on mobile, making all text inside appear blurry/fuzzy. Apply blur, backdrop-filter, and glass effects ONLY to purely decorative background elements (blobs, orbs, noise overlays, scene layers) that contain NO text. For cards, panels, and sections that contain text, use solid dark backgrounds (e.g., `bg-[#111827]`, `bg-[#0d1117]`) or gradient backgrounds without backdrop-filter. This is the single most impactful fix for mobile text sharpness. Confidence: 0.95

- Admin edit modals should use `max-h-[90vh] overflow-y-auto` to constrain tall forms within the viewport and enable internal scrolling. Confidence: 0.75
- Admin layout main content area should have `overflow-y-auto h-screen` so the page content scrolls independently within the layout, not with the page. Confidence: 0.70
- Admin CRUD sections should support file uploads (PDFs, images) with a visual preview showing how the uploaded file will appear on the frontend UI, plus a dummy/preview placeholder when no file is present. Confidence: 0.75
- The portfolio chatbot/AI assistant should display all information as short, scannable bullet points entirely inside the chat window — it must never suggest or trigger page-scrolling behaviors, and should keep responses concise and compact without long paragraphs. The chatbot should know and answer directly about the core tech stack (React.js, TypeScript, Tailwind CSS, Node.js & Express.js, PostgreSQL & Supabase, Docker & Vercel Deployment). Confidence: 0.85

- Chatbot responses must follow a strict Markdown formatting structure: (1) every heading (`###`) isolated on its own line with a blank line before and after — never merged into previous text; (2) bullet lists use `- ` prefix with each item on its own line, blank line before/after the list, and items under 15 words; (3) bold labels at the start of key lines (`**Skill:** React.js`) for scannability; (4) sentences kept under 15 words with no wall-of-text paragraphs; (5) blank lines between every section. On the frontend, the ChatMarkdown component must explicitly render headings (`h1`-`h4`) with proper margin/spacing classes so the LLM's Markdown spacing isn't collapsed into a single text block. Confidence: 0.85
- When a scrollable container (`overflow-y-auto`) is inside a `flex-col` parent, always add `min-h-0` (or equivalent `min-height: 0`) to the scrollable child. This overrides flexbox's default `min-height: auto` which otherwise prevents the child from shrinking below its content height, killing the scroll. Confidence: 0.85
- On scrollable containers inside overlays/panels (chatbot messages, modals, etc.), add `overscroll-contain` to prevent touch scroll chaining where swiping at the container's scroll boundary scrolls the page behind it. Confidence: 0.80
- For scrollable areas inside dark-themed overlays (chatbot, modals, sidebars), style scrollbars with a thin profile (4px width, `scrollbar-width: thin`), transparent track, and a faint semi-transparent white thumb that becomes more visible on hover — instead of completely hiding scrollbars. This provides a professional feel while keeping the UI clean. Confidence: 0.80

- When a sidebar or panel contains a scrollable messages/content area, forward mouse wheel (`onWheel`) and touch (`onTouchStart`/`onTouchMove`) scroll events from the entire sidebar surface to the internal scrollable container, so scrolling works regardless of which part of the sidebar the user interacts with. Check scroll boundaries so events pass through naturally when the container is at the top or bottom. Confidence: 0.85

- On mobile, scale down all card elements (padding, icons, avatar sizes, text, gaps) proportionally using `sm:` breakpoint classes — e.g., `p-4 sm:p-6`, `w-8 sm:w-10`, `text-xs sm:text-sm`, `gap-2 sm:gap-3`, `size={14} sm:size={18}`. This prevents cards from looking oversized/top-heavy on small screens. Confidence: 0.80

- On mobile, contact section elements should stack **vertically in a single column** with each element at 100% width — cards become full-width, inputs fill the container, no horizontal scrolling, no overflow, no fixed heights, everything adapts naturally. The order should be: Heading → Description → Form → Contact Cards → WhatsApp Button. Avoid 2-column or side-by-side card layouts on mobile viewports. Desktop uses the standard two-column grid (form left, cards right). The column wrapper should be centered on mobile with `flex flex-col mx-auto w-full max-w-lg`, and the desktop positioning must be explicitly restored with `lg:max-w-none lg:mx-0`. Contact info cards on mobile follow a consistent horizontal layout pattern: `display: flex; align-items: center; gap: 16px;` with icon on the left, text on the right, using `border-radius: 16px` and `padding: 20px`. Avoid `motion.div` wrappers that add layout-shifting animations (like `whileFocus={{ scale: 1.01 }}`) on form inputs — these cause janky resizing on mobile. Ensure all elements within the same visual row/section share a uniform border-radius for visual consistency. Confidence: 0.90
- Form submit buttons should be proportionally sized to match the input fields above them — not oversized or blocky relative to the inputs. The button height should closely match the input field height (e.g., `min-h-[44px]` matching input `min-h-[44px]`), and the button should span the full width of the form column for visual harmony. Avoid tall, massive submit buttons that visually dominate the inputs. Confidence: 0.70

- On dark-themed cards containing text content, use a clear contrast hierarchy: labels/headings in a muted tone (e.g., `text-text-muted` or `#94A3B8` with uppercase tracking) and the actual value/content in a high-visibility tone (e.g., `text-text-primary` or `#CBD5E1` white). This ensures data values are immediately legible while labels provide clean visual structure without competing for attention. Confidence: 0.85

- Use `leading-tight` on headings on mobile so multi-line titles don't have excessive line-height spacing, creating a tighter, more polished appearance. Confidence: 0.70

- Certificate/certification viewer pages should not block screenshots or apply any security restrictions — users should be able to freely view, screenshot, and interact with certificates without blurred overlays, right-click blocking, keyboard shortcut prevention, or any other protection measures. Confidence: 0.85

- Certificate viewer pages should have a professional top bar showing the cert title and issuer name on the left, and a "Verify" link/button on the right (linking to the external credential URL) — not an ultra-minimal UI with only a back arrow. The overall design should feel polished, open, and professional. Confidence: 0.20

- Back navigation from certificate/detail viewer pages should use React Router's `state` (e.g., `state={{ tab: 'certifications' }}`) to land users on the relevant section or tab of the parent page, not just the generic parent route. Confidence: 0.85

- Dedicated viewer/detail pages (certificate viewer, portfolio viewer, etc.) should be standalone routes outside the main site layout, with their own navigation bar (back button, zoom/controls, action buttons) rather than inheriting the site's navbar and footer — giving a full-screen, focused viewing experience. Confidence: 0.80

- In admin CRUD forms, provide both file upload buttons AND URL text inputs for attaching documents/PDFs/images (supports Google Drive links, CDN URLs, etc.) — not just file upload. The preview area should show a high-fidelity mock of the exact public-facing card/component layout (same badges, labels, hover effects) rather than a simple thumbnail. Confidence: 0.75

- For in-browser PDF viewing (certificates, documents), prefer frontend-side PDF rendering (react-pdf) over iframe embedding — controls zoom, page navigation, fullscreen, and allows disabling download/print. Render PDFs on a dark background matching the site's theme. Support mouse wheel scrolling to flip between pages (with an accumulated delta threshold, e.g. 80px, to avoid accidental flips) and touch swipe gestures (minimum 60px horizontal swipe, filtering out vertical scrolling) for mobile-friendly page navigation. Confidence: 0.85

- For security-sensitive views (certificates, credentials, protected documents), implement comprehensive screenshot/download prevention: block PrintScreen key, F12/DevTools shortcuts, Ctrl+S/P/U, right-click context menu, copy/cut/drag/drop events, touch long-press save-image; add a CSS screenshot deterrent overlay (subtle repeating grid pattern invisible to the eye but visible in captures); hide content when printing via `@media print { display: none }`; disable user-select and user-drag on all images/canvases/iframes; sandbox iframes without allow-downloads; and most importantly, hide/replace content with a blurred overlay on `window.blur` and `document.visibilitychange` events to prevent screenshots taken by switching tabs or minimizing the window. Confidence: 0.30

- For dedicated viewer pages (certificates, credentials), prefer a slim top bar showing certificate metadata (title, issuer, date) above the content area rather than a left sidebar — the heading sits above the document, leaving the full width for the document. Confidence: 0.85

- For dedicated viewer pages (certificates, credentials), remove any "Verify"/external link buttons that open content in a new tab. The viewer should be self-contained within the app with no way to navigate away to external sites. Confidence: 0.85

- Certificate/document viewer pages should render edge-to-edge: fixed inset-0 with no padding, no margins, no border blocks, no card containers, no rounded corners, no external framing whatsoever — the content fills 100vw × 100vh with the background color extending to the absolute edges. All navigation chrome (back button, controls) should float as overlays on top of the content rather than taking up layout space. Confidence: 0.90

- On full-screen certificate/document viewer pages, the floating navigation chrome (back arrow, page counter, controls) should auto-hide after ~3 seconds of inactivity and fade back in on mouse movement, click, or keypress — keeping the document completely unobstructed during passive viewing. Confidence: 0.80

- For in-browser PDF viewing in full-screen contexts, use react-pdf's native `width={pageWidth}` prop (bound to `window.innerWidth`) instead of manually computing a `scale` value — this provides true dynamic page-width fitting that automatically adjusts on resize without capped max-widths or padding subtractions. Confidence: 0.85

- When embedding Google Drive documents (PDFs) via iframe in full-screen viewer pages, strip the native toolbar/external-link button with a triple-layer approach: (1) append `?rm=minimal&embedded=true` to the embed URL to tell Google to minimize the toolbar, (2) clip the bottom ~48px of the iframe container (`clipPath: 'inset(0 0 48px 0)'`) while expanding the iframe 48px taller to compensate (`marginBottom: '-48px'`, `height: 'calc(100% + 48px)'`), and (3) place an invisible click-interceptor div over the bottom-right toolbar area to block any remaining stray clicks. Confidence: 0.85
